# WS9 — The Jobs / Thiel loop, and polish

> Scoped in `2026-09-05-wingman-v4.md` § WS9. Gate: both scorecards ≥ 9.5/10 in the same round. Each round is logged in `docs/reviews/2026-09-05-feedback.md` under *Iteration log*.

## Round 1 — measure the baseline (after WS0–WS8)

Score every criterion against evidence: an e2e spec, a grep, a build line, a screenshot. No criterion scores above 9 without a command that proves it.

## Round 2 — fix what Round 1 found

1. **The signature moment (J6).** `screens/inbox/arrive.ts` marks a room as arriving when a request is accepted; `ChannelScreen` takes the mark once on mount and never again. CSS: the two presence cards slide in from opposite sides and lock, the terminal stamp turns in off-axis, the "both in the same terminal" line draws itself. 1400 ms total. Reduced motion collapses every keyframe to its last frame. Proven by `e2e/accept.spec.ts` (plays once, not on return).
2. **Feels fast (J7).** Press state on `.tab` (glyph only, the bar is fixed), `.segmented__item`, `.menu__item`. Route-level `React.lazy` for every screen but the board; `cities` and `vendor` chunks so the never-changing tables cache across releases. Hero image as a 1200 px WebP (187 KB → 64 KB).
3. **Copy has one voice (J8).** Every em-dash flourish in a UI string replaced with a full stop, a colon or a comma. "Submit", "Error", "Success", "Please": none existed; the grep stays in the exit check.
4. **Accessible.** `--faint` was 2.7:1 on the light canvas and 3.8:1 on dark; now 4.6:1 and 4.7:1 against the surfaces it sits on. Every tappable element measured on Discover, Inbox, You and Person: nothing under 44 px to the thumb (the two 36 px controls carry an extended hit area, the same device the filter chips already used).
5. **Zero dead ends (Jobs 3).** `e2e/dead-ends.spec.ts` walks eighteen routes, including four not-found states, and asserts an action outside the tab bar on each.
6. **Requests show who is asking.** The incoming strip carries the sender's stamps (redacted at rung 1 in a selector, never a `Person` in the screen), the proposal, and their sentence.
7. **C8.** The receipt leads with the usable window: "42 min together, connecting through Singapore". The `dl` rows became one sentence (J5).
8. **Thiel 2 and 4.** A "Why now" card in the memo and on the organiser page; a memo appendix stating what changed since v3 and restating team and dependencies.
9. **Both themes (J9).** `SHOOT_DARK=1 npm run shoot` shoots every screen in both themes on the phone viewport; twelve dark screens reviewed by eye.

## Exit

`verify`, `lint`, `build`, `e2e` green; both scorecards ≥ 9.5 in the log; deferred items named (C5 `excludeDomains`, the 101 KB city table).
