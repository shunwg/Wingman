# Persona roll-up — 7 September 2026

Three AI personas walked 43 real screenshots of the v6 build (six flows, `testing/flows/`, shot at 402 × 874) under the runner prompt in `testing/prompts/persona-runner.md`. Reports: `testing/reports/2026-09-07/{nervous-flyer,privacy-wary,bad-actor}.md` (generated, not committed). This file is the durable record.

Ranking rule: **P0** = flagged by three or more personas, or *any* safety/privacy flag · **P1** = blocking for one persona · **P2** = friction only. Severity beats volume.

## P0 — fixed before merge

| Issue | Personas | Where | Fix |
|---|---|---|---|
| The board printed `hiddenByFilters` as an exact integer ("19 people are hidden by the filters above", "16 more people hidden"). With the bucketed context line beside it, subtraction gave exact intersections — the bad actor's attack 2 worked. | privacy-wary, bad-actor | `src/screens/discover/BoardScreen.tsx` | Both lines go through `bucketPhrase()` (rule 3). |
| "Heading about 3km from you" on a card before anyone has said yes — a coarse location, disclosed pre-consent, rounded against an origin the viewer chooses. | privacy-wary, bad-actor | `BoardScreen.tsx` `CardFooter` | Direction, never distance: "Heading the same way as you." under 5 km, otherwise "the same city, a different part." |

## P1 — fixed before merge

| Issue | Personas | Where | Fix |
|---|---|---|---|
| Blank page after deleting and reloading — indistinguishable from a crash; for Kari, "I broke it." | all three | `routes.tsx` (lazy `WelcomeScreen`, `Suspense fallback={null}`); the flow shot before the chunk landed | Welcome is eager: it is the first paint for a stranger. Flow 06 keys on the welcome button before shooting. |
| Deletion is called "Sign out and start over" / "Sign out" while the copy says nothing is kept. | all three | `YouScreen.tsx` | "Delete my profile" → "Delete your profile?" → "Delete". Flow updated. |
| Unticking the last "who you want to meet" option refused silently. | all three | `MeetPreference.tsx` | A status line: "One has to stay on. Untick a different one, or leave it at everyone." |
| "What you do" signup step: fourteen fields, "Skip for now" below the fold, no sentence saying a leisure traveller can leave it blank. | nervous-flyer | `WorkStep.tsx` | **Open.** Next round: lead with one sentence and put Skip in the header. |

## P2 — logged

- Tab bar appears over form fields and the composer in the screenshots. This is a full-page-capture artefact (the fixed bar is painted at the viewport's bottom on a tall page); `shell__main` reserves `--tabbar-height + 24px` under content. Verified on device size; no change.
- "Stand-in" badge on mocked providers is undefined on the verify step (nervous-flyer, privacy-wary). Add one sentence.
- No visible cap on the room composer; the field simply stops at 240 (all three). Add a counter at 200+.
- Two date formats on the trip form (ISO in the error, dd.mm.yyyy in the field) (nervous-flyer).
- Demo trips appear in a demo user's Trips (nervous-flyer) — by construction of the demo; the first-run path is clean.
- "Women only" board lens shares its label with the two-way policy; the asymmetry note appears only when the chip is on (privacy-wary). Rename the lens "Show women only".
- Compact stamp icons on cards carry no label (nervous-flyer) — the person page labels them; add `title` on the compact badge.

## The bad actor's seven attacks

| # | Attack | Result | Evidence |
|---|---|---|---|
| 1 | Change own gender to widen the pool | **Partly worked** | Gender is self-declared and the screen says so ("BankID does not tell us your gender"). Documented decision: a national-ID gender attestation is a disclosure the stamps contract forbids. The preference still works both ways, so lying widens *who he sees* and *who sees him* together. Logged, not changed. |
| 2 | Narrow filters until a count identifies a person | **Worked → fixed** | Raw `hiddenByFilters`. Now bucketed. |
| 3 | See name / company / links / terminal before a yes | Failed | First name, headline, role, stamps only. |
| 4 | Infer the other side's accept sheet | Failed | The sheet lists what *she* reveals; nothing about him is shown to her before a yes. |
| 5 | Terminal or location in the room before an update | Failed | "Location appears once they post an update" holds; "LHR · T2" appears only after his own tap. |
| 6 | Use the audience preview to reason about the board | Failed | Preview renders archetypes, not people. |
| 7 | Still see her after being hidden | Untestable from screenshots | Hides are device-local until a backend exists; deleting and re-creating resets them. Already in the launch memo's server scope. |

## Verdict

No safety flag survives the merge. One P1 (the work step) and seven P2s are carried to the next round. `npm run verify` 388 tests, `npm run e2e` 16 passed, `npm run personas:flows` 6 passed at commit time.
